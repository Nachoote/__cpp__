/* 1. Declarar tipo de datos string = Array de caracteres con terminacion null(\0) (Referir a documentacion del curso) */


/* Escribir funcionalidad para leer texto desde la entrada(scanf) */


/* 2. Validar oraciones. si oracion[0] == mayuscula && oracion[TAM -1 ] == oracion validada.*/


/* 3. Solicite al docente una cantidad máxima de palabras para el trabajo. A partir del texto ingresado,
calcule la cantidad total de palabras que lo componen y avise al docente: cuantas palabras posee, y
(en caso de excederse) por cuantas palabras está pasado el texto respecto a lo pretendido por el
docente.
Tener en cuenta que las palabras pueden estar separadas por uno o más espacios en blanco, y
además tener signos de puntuación, los cuales no se consideran palabras. En el ejemplo propuesto,
el texto cuenta con 25 palabras.
*/


/* 4. Solicite al docente una palabra clave del tema y determine si se utilizó en el texto.
Al momento de comparar palabras, no se harán distinciones entre minúsculas y mayúsculas. Por
ejemplo, si el usuario ingresa PROGRAMACIÓN (todo en mayúsculas), el programa responderá que
dicha palabra efectivamente se encuentra en el texto. En cambio, si el usuario ingresa Sistema, el
programa responderá que no existe dentro del trabajo. */
